import { Story, Meta } from '@storybook/react'
import CategorySliderItem from '.'

export default {
  title: 'CategorySliderItem',
  component: CategorySliderItem
} as Meta

export const Default: Story = () => <CategorySliderItem />
