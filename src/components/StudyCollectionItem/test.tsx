import { render } from 'utils/test-utils'

import StudyCollectionItem from '.'

describe('<StudyCollectionItem />', () => {
  it('should render the heading', () => {
    render(<StudyCollectionItem />)
  })
})
