import { Story, Meta } from '@storybook/react'
import CategoryTemplate from '.'

export default {
  title: 'CategoryTemplate',
  component: CategoryTemplate
} as Meta

export const Default: Story = () => <CategoryTemplate />
