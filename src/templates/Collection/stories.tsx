import { Story, Meta } from '@storybook/react'
import Collection from '.'

export default {
  title: 'Collection',
  component: Collection
} as Meta

export const Default: Story = () => <Collection />
